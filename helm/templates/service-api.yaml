apiVersion: v1
kind: Service
metadata:
  name: omni-query-api
  namespace: omni-query
spec:
  selector:
    app: omni-query-api
  ports:
    - port: {{ .Values.service.api.port }}
      targetPort: 8000
  type: {{ .Values.service.api.type }}
